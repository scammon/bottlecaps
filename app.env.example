# Copy to app.env and fill in. Generate a key pair with:
#   node -e "console.log(require('web-push').generateVAPIDKeys())"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# Informational per the Web Push spec -- a contact URL or mailto:.
VAPID_SUBJECT=https://example.com
